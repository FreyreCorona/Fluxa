tailscale_auth_key: "{{ vault_tailscale_auth_key }}"

platform_dir: /opt/platform
platform_user: ubuntu

base_packages:
  - curl
  - wget
  - git
  - jq
  - htop
  - unzip
  - ca-certificates
  - gnupg
  - lsb-release
  - ufw
