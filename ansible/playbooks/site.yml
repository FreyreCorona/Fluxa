- name: Common bootstrap — all nodes
  hosts: all
  gather_facts: true
  become: yes
  roles:
    - common

- name: Edge node setup
  hosts: edge
  become: yes
  roles:
    - k3s
    - traefik
    - postgres
    - go-api

- name: Worker node setup
  hosts: worker
  become: yes
  roles:
    - k3s
    - runtime-agent

- name: Infra node setup
  hosts: infra
  become: yes
  roles:
    - gitea
    - docker-registry
    - victoriametrics
    - uptime-kuma
